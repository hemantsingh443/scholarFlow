# OpenRouter API Key (required)
# Get one free at https://openrouter.ai/
OPENROUTER_API_KEY=sk-or-v1-your-api-key-here

# Optional: Frontend URL for CORS (Production)
FRONTEND_URL=https://your-frontend.vercel.app

# === MEMORY OPTIMIZATION (for free tier hosting) ===

# Set to "true" for Render/Railway free tier (512MB RAM)
# Enables SKIP_VECTOR_STORE automatically
LIGHTWEIGHT_MODE=false

# Skip ChromaDB vector storage entirely (saves ~200MB RAM)
# Papers are still parsed and summarized, just not stored for retrieval
# Quality is maintained - summaries go directly to the report
# Set explicitly, or leave unset to inherit from LIGHTWEIGHT_MODE
# SKIP_VECTOR_STORE=true

# Optional: ChromaDB persistence directory (only used if SKIP_VECTOR_STORE=false)
CHROMA_PERSIST_DIR=./chroma_db

# Optional: Custom embedding model
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
